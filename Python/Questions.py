# Q Here's a Python program to get the longest unique substring (substring with all distinct characters) from a given string:
# Q To convert an integer to a string without using built-in methods in Python, you can implement your own logic using arithmetic operations and character manipulation. Here's an example:
    # eg 123 -> "123"
# Q write a function to reverse only alphabets not special character from below string
    # input_data = "Hell! wor?d" # reverse the string only alphabet
# Q Here's a Python program to rearrange the list so that it alternates between positive and negative numbers
# Q Find the largest element from the list
# Q Find the second largest element form the list
# Q Right rotate list by k place
# Q Move zeros at the end of the list in place
# Q Print the string in reverse excluding the special characters and need to preserve its index as well inreverse
    # input = "pr@ka#$%sh*"
    # output = hs@ak#$%rp*
# Q Remove duplicate from the sorted Array nums = [0, 0, 1, 1, 1, 2, 2, 3, 3, 4] -> [0, 1, 2, 3, 4, _, _, _, _, _]
# Q Given an array containing n distinct no in the range [0,n] retru thr only no of in the range that is missing from array
# Q Given a binary array , return thr maxm no if consectuive 1's in the array eg [1,1,0,0,1,1,1] -> 3
# Q Given an array of the integer and integer target return tindices if the 2 no such that they add upto target
# Q Sort an array that contains only 2 distinct values 0,1,2 so that all 0's come first then all 1's and finally 2's Duthc National flag
# Q Find the sub array of an array whose element add up to largest possible sum
# Q Given an array nums of size n return the majority elements, the majority element is the element that appears more than [n/2] times nums = [3,2,3] -> 3 nums = [2,2,1,1,1,2,2] -> 2
# Q You are given an array prices where prices[i] is the price of a given stock on the ith day. 
    #  You want to maximize your profit by choosing a single day to buy one stock and choosing a different day in the future to sell that stock.
    # Return the maximum profit you can achieve from this transaction. If you cannot achieve any profit, return 0.

# Q You are given a 0-indexed integer array nums of even length consisting of an equal number of positive and negative integers. [3,1,-2,-5,2,-4] -> [3,-2,1,-5,2,-4]
# Q Given an array of size n an element is called an array leaders if it is greater than or equal to every element on its right 
# Q Given an unsorted array of integers nums, return the length of the longest consecutive elements sequence.
#  Q Find the anagrams words from a given list of words like words = ["gum", "mug", "eat", "ate", "tan"]
# Q Find the last repeating character in string 
# Q Reverse the word in the sentenace without affecting the special characters 
        # sentence = "Hey, how are you doing ?"

# Q input s = "TTTTTTttttt6667aaaaaAAAA557BBBBBbbbbbb33338888000LLLLL099eeeEEE" output = "TaBLe"
# Q Write the python code for the below input and output Eg. Input string = “aabbcccaaaa” Output of code = “a2b2c3a4”
  


# DSA Questions 
# Extraction of Digits 
# Python program to count no of digit
